server:
  port: 8098
spring:
  application:
    name: eureka-service
  cloud:
    config:
      enabled: true
      uri: http://127.0.0.1:8068
      name: application-demo
     # profile: dev

endpoints:
  restart:
    enabled: true