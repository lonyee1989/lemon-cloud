spring:
  application:
    name: account
  cloud:
    config:
      enabled: true
      uri: http://127.0.0.1:8068
      name: application-account
      # profile: dev

endpoints:
  restart:
    enabled: true