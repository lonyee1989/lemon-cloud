server:
  port: 8091
  use-forward-headers: false
spring:
  application:
    name: file-service
  cloud:
    config:
      enabled: true
      uri: http://127.0.0.1:8068
      name: application-fileservice

endpoints:
  restart:
    enabled: true