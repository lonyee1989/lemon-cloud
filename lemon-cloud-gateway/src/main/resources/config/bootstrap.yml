spring:
  aop:
    proxyTargetClass: true
  application:
    name: gateway
  cloud:
    config:
      enabled: true
      uri: http://127.0.0.1:8088
      name: application-gateway
      # profile: dev