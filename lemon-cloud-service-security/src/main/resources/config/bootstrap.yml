server:
  port: 8091
spring:
  application:
    name: security-service
  cloud:
    config:
      enabled: true
      uri: http://127.0.0.1:8068
      name: application
      profile: dev

endpoints:
  restart:
    enabled: true